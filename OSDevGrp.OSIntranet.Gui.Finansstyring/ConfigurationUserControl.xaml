<UserControl
    x:Class="OSDevGrp.OSIntranet.Gui.Finansstyring.ConfigurationUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OSDevGrp.OSIntranet.Gui.Finansstyring"
    xmlns:vm="using:OSDevGrp.OSIntranet.Gui.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    
    <UserControl.Resources>
        <vm:MainViewModel x:Key="MainViewModel"/>
    </UserControl.Resources>
    
    <Grid DataContext="{Binding Source={StaticResource MainViewModel}, Path=FinansstyringKonfiguration}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="5,5,5,5">
            <TextBlock Margin="0,0,0,5" Style="{StaticResource ItemTextStyle}" Foreground="Black" Text="{Binding Path=FinansstyringServiceUriLabel}"/>
            <TextBox BorderBrush="Black" BorderThickness="1,1,1,1" Text="{Binding Path=FinansstyringServiceUri, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>
</UserControl>
